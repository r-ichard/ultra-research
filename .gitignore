# Python
__pycache__/
*.py[cod]
*.egg-info/

# Local research OUTPUT only — root-anchored so it can NEVER match the
# skills/research/ skill directory (an unanchored `research/` would).
/research/

# Editor / OS cruft
.DS_Store
